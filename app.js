const translations = {
  pt: {
    pageTitle: "Pedro Vitor Magalhães da Silva | Portfolio",
    pageDescription:
      "Portfólio de Pedro Vitor com foco em dados, IA aplicada, operação e direção visual.",
    skipToContent: "Pular para o conteúdo",
    brandRole: "Dev + IA + Design",
    navExperience: "Experiência",
    navSkills: "Habilidades",
    navEducation: "Formação",
    navContact: "Contato",
    navProjects: "Projetos",
    langButton: "EN",
    availabilityPill: "Dados, automação e direção visual em uma base só.",
    availabilityStatus: "Disponível para oportunidades",
    eyebrow: "Disponível para oportunidades remotas e presenciais",
    heroName: "Pedro Vitor\nMagalhães da Silva",
    heroKicker: "Dev, IA aplicada e direção visual para projetos digitais.",
    statOneValue: "Dados + operação",
    statOneLabel: "Perfil orientado a execução, organização e análise.",
    statTwoValue: "IA prática",
    statTwoLabel: "Automação e apoio a fluxos internos com ferramentas atuais.",
    heroText:
      "Sou o Pedro — misturo dados, automação e design pra resolver problemas reais. Gosto de transformar bagunça em estrutura e ideias em coisa visual que funciona. Quando não tô codando, tô desenhando ou treinando IA pra fazer o trabalho chato por mim.",
    primaryCta: "Falar por e-mail",
    secondaryCta: "Ver LinkedIn",
    downloadCv: "Baixar Currículo",
    scrollHint: "Role para explorar",
    signalAutomation: "Automação",
    signalDesign: "Design",
    avatarPlaceholder: "Sua foto",
    profileLocation: "Unaí, Minas Gerais · Brasil",
    profileRole: "Data Science Student · AI Support · Graphic Design",
    summaryLabel: "Foco atual",
    focusOneTitle: "Dados aplicados",
    focusOneText: "Estruturação, limpeza e apoio a relatórios.",
    focusTwoTitle: "IA prática",
    focusTwoText: "Uso de IA para acelerar processos internos.",
    focusThreeTitle: "Design visual",
    focusThreeText: "Materiais gráficos para comunicação e presença digital.",
    introLabel: "Sobre",
    introTitle: "Construindo pontes entre dados, automação de processos e design visual.",
    introText:
      "Atuo na intersecção entre a análise técnica e a comunicação visual. Com foco em Ciência de Dados, desenvolvo automações inteligentes e arquiteturas de informação robustas, sem abrir mão de uma identidade visual refinada. Meu objetivo é transformar processos complexos em fluxos de trabalho eficientes e intuitivos.",
    introDetailData: "Desenvolvimento de bases estruturadas, scripts ETL e análise analítica para suporte à tomada de decisões operacionais.",
    introDetailAi: "Integração de APIs de IA generativa, engenharia de prompt e scripts de automação para otimizar fluxos de trabalho.",
    expLabel: "Experiência",
    expTitle: "Experiências recentes e complementares",
    job1Title: "Cientista de Dados & Apoio Operacional",
    job1Meta: "Goiânia/GO · Prestador de serviços (PJ)",
    job1Bullet1: "Estruturei +500 registros financeiros, reduzindo tempo de consulta em relatórios internos.",
    job1Bullet2: "Automatizei cálculo de preço de custo de importação com planilhas inteligentes.",
    job1Bullet3: "Criei base de dados organizada que virou referência para consultas do time.",
    job1Bullet4: "Treinei modelos de IA para automatizar processos repetitivos da operação.",
    job2Title: "Experiência Complementar em Design Gráfico",
    job2Meta: "Goiânia/GO · Maio a Outubro",
    job2Bullet1: "Apoio na produção de materiais visuais e organização de conteúdos.",
    job2Bullet2: "Uso de Photoshop em atividades internas de comunicação visual.",
    job3Title: "Jovem Aprendiz — Administrativo",
    job3Meta: "Goiânia/GO · Junho de 2024 a Maio de 2025",
    job3Bullet1: "Apoio em rotinas administrativas e organização de documentos.",
    job3Bullet2: "Suporte às demandas internas com foco em organização e atendimento.",
    job3Bullet3: "Auxílio em atividades operacionais e controle de informações.",
    skillsLabel: "Habilidades",
    skillsTitle: "Ferramentas e áreas de atuação",
    skillDataScience: "Ciência de Dados",
    skillAutomation: "Automação",
    skillOffice: "Excel / Pacote Office",
    skillGraphicDesign: "Design Gráfico",
    skillMarketing: "Marketing Digital",
    skillObsidian: "Obsidian (Gestão de Conhecimento)",
    skillPythonLibraries: "Bibliotecas Python (NumPy, Scikit-Learn, etc.)",
    skillEtl: "ETL / Tratamento de Dados",
    languagesTitle: "Idiomas",
    langPt: "🇧🇷 Português (Nativo)",
    langEn: "🇺🇸 Inglês (Intermediário)",
    langEs: "🇪🇸 Espanhol (Intermediário)",
    refTitle: "Referências & Inspiração",
    refArt: "Arte",
    refArtVal: "Minimalismo, Bauhaus, Editorial",
    refMusic: "Música",
    refMusicVal: "Ambient, Lo-Fi, Synthwave",
    refCinema: "Cinema",
    refCinemaVal: "Sci-Fi, Cyberpunk, Neo-noir",
    refBooks: "Livros",
    refBooksVal: "Ficção Científica, Design de Info",
    refGames: "Jogos",
    refGamesVal: "Puzzle, Indies, Direção de Arte",
    projectsLabel: "Projetos",
    projectsTitle: "Projetos reais já publicados",
    projectsIntro:
      "Repositórios que já mostram prática com dados, interface, automação e experimentação técnica.",
    project1Status: "Publicado",
    project1Title: "Banco de Dados Agrícola",
    project1Text:
      "Sistema de estruturação de dados agrícolas com tabelas otimizadas, queries de análise e visualização de safras.",
    project2Status: "Publicado",
    project2Title: "Interface Visual para CLI",
    project2Text:
      "Terminal customizado com GUI QML, tema Nordic e gerenciamento visual de processos no Linux.",
    project3Status: "Publicado",
    project3Title: "Rede Neural Faux",
    project3Text:
      "Rede neural construída do zero em Python — arquitetura, treinamento e visualização de aprendizado.",
    projectLink: "Ver no GitHub",
    visualLabel: "Visual",
    visualTitle: "Trabalhos gráficos e presença criativa",
    visualIntro:
      "Meu perfil no Behance concentra a parte visual do meu portfólio. Aqui deixo o acesso direto e espaço para destacar peças específicas na próxima atualização.",
    visualProfileLabel: "Destaque",
    visualProfileTitle: "Trabalhos Vem Ser Tahto",
    visualProfileText:
      "Peças visuais criadas durante minha experiência com design gráfico — identidade, social media e materiais internos. Tudo reunido no Behance.",
    visualProfile2Title: "Projeto Visual",
    visualProfile2Text:
      "Portfólio de identidade visual, tipografia e composições gráficas autorais publicadas no Behance.",
    visualPrimaryCta: "Abrir projeto no Behance",
    visualSecondaryCta: "Ver todos os repositórios",
    visualNextLabel: "Behance",
    visualNextTitle: "Adicionar mais peças com contexto",
    visualNextText:
      "Posso seguir trazendo mais trabalhos do Behance para cá, um por card, com descrição curta, tipo de peça e link direto.",
    educationLabel: "Formação",
    educationTitle: "Ciência de Dados em andamento",
    educationText: "Anhanguera · graduação em curso com foco em base analítica, dados e tecnologia.",
    certLabel: "Certificação",
    roadmapLabel: "Próximos passos",
    roadmapTitle: "Evoluções planejadas para a versão publicada",
    roadmap1Title: "Projetos reais",
    roadmap1Text: "Adicionar scripts Python, automações, dashboards e estudos com IA.",
    roadmap2Title: "Portfólio visual",
    roadmap2Text: "Incluir peças de design gráfico e social media com imagens e contexto.",
    roadmap3Title: "Publicação",
    roadmap3Text: "Hospedar no GitHub Pages, Vercel ou Netlify com domínio próprio.",
    contactLabel: "Contato",
    contactTitle: "Vamos conversar sobre trabalho, projetos ou colaboração.",
    contactText:
      "Se a vaga ou projeto precisa de alguém que organize, execute e aprenda rápido, esse é o melhor ponto de partida.",
    footerQuote: "\"Os sonhos de um homem não têm fim.\"",
    footerText:
      "Portfólio pessoal de Pedro Vitor. Última atualização em 31 de maio de 2026.",
    footerStatus: "Disponível para oportunidades",
    floatingCta: "Falar comigo"
  },
  en: {
    pageTitle: "Pedro Vitor Magalhaes da Silva | Portfolio",
    pageDescription:
      "Pedro Vitor's portfolio focused on data, applied AI, operations and visual direction.",
    skipToContent: "Skip to content",
    brandRole: "Dev + AI + Design",
    navExperience: "Experience",
    navSkills: "Skills",
    navEducation: "Education",
    navContact: "Contact",
    navProjects: "Projects",
    langButton: "PT",
    availabilityPill: "Data, automation and visual direction in one base.",
    availabilityStatus: "Available for opportunities",
    eyebrow: "Available for remote and on-site opportunities",
    heroName: "Pedro Vitor\nMagalhães da Silva",
    heroKicker: "Dev, applied AI and visual direction for digital projects.",
    statOneValue: "Data + operations",
    statOneLabel: "Profile oriented toward execution, organization and analysis.",
    statTwoValue: "Practical AI",
    statTwoLabel: "Automation and support for internal workflows with current tools.",
    heroText:
      "I'm Pedro — I mix data, automation, and design to solve real problems. I love turning chaos into structure and ideas into visual things that work. When I'm not coding, I'm designing or training AI to do the boring stuff for me.",
    primaryCta: "Send an email",
    secondaryCta: "View LinkedIn",
    downloadCv: "Download CV",
    scrollHint: "Scroll to explore",
    signalAutomation: "Automation",
    signalDesign: "Design",
    avatarPlaceholder: "Your photo",
    profileLocation: "Unai, Minas Gerais · Brazil",
    profileRole: "Data Science Student · AI Support · Graphic Design",
    summaryLabel: "Current focus",
    focusOneTitle: "Applied data",
    focusOneText: "Structuring, cleaning and supporting reports.",
    focusTwoTitle: "Practical AI",
    focusTwoText: "Using AI to accelerate internal processes.",
    focusThreeTitle: "Visual design",
    focusThreeText: "Graphic materials for communication and digital presence.",
    introLabel: "About",
    introTitle: "Building bridges between data, process automation, and visual design.",
    introText:
      "I operate at the intersection of technical analysis and visual communication. With a focus on Data Science, I build smart automations and robust information architectures, without compromising on refined visual identity. My goal is to transform complex workflows into efficient, intuitive, and high-impact solutions.",
    introDetailData: "Developing structured databases, ETL scripts, and analytical insights to support operational decision-making.",
    introDetailAi: "Integrating generative AI APIs, prompt engineering, and automation scripts to optimize workflows.",
    expLabel: "Experience",
    expTitle: "Recent and complementary experience",
    job1Title: "Data Scientist & Operational Support",
    job1Meta: "Goiania, GO · Contractor",
    job1Bullet1: "Structured 500+ financial records, reducing lookup time for internal reports.",
    job1Bullet2: "Automated import cost price calculations with smart spreadsheets.",
    job1Bullet3: "Built an organized database that became the team's go-to reference.",
    job1Bullet4: "Trained AI models to automate repetitive operational processes.",
    job2Title: "Complementary Experience in Graphic Design",
    job2Meta: "Goiania, GO · May to October",
    job2Bullet1: "Supported the production of visual materials and content organization.",
    job2Bullet2: "Used Photoshop in internal visual communication tasks.",
    job3Title: "Administrative Apprentice",
    job3Meta: "Goiania, GO · June 2024 to May 2025",
    job3Bullet1: "Supported administrative routines and document organization.",
    job3Bullet2: "Helped internal demands with focus on organization and service.",
    job3Bullet3: "Assisted operational tasks and information control.",
    skillsLabel: "Skills",
    skillsTitle: "Tools and areas of work",
    skillDataScience: "Data Science",
    skillAutomation: "Automation",
    skillOffice: "Excel / Office Suite",
    skillGraphicDesign: "Graphic Design",
    skillMarketing: "Digital Marketing",
    skillObsidian: "Obsidian (Knowledge Management)",
    skillPythonLibraries: "Python Libraries (NumPy, Scikit-Learn, etc.)",
    skillEtl: "ETL / Data Wrangling",
    languagesTitle: "Languages",
    langPt: "🇧🇷 Portuguese (Native)",
    langEn: "🇺🇸 English (Intermediate)",
    langEs: "🇪🇸 Spanish (Intermediate)",
    refTitle: "References & Inspiration",
    refArt: "Art",
    refArtVal: "Minimalism, Bauhaus, Editorial",
    refMusic: "Music",
    refMusicVal: "Ambient, Lo-Fi, Synthwave",
    refCinema: "Cinema",
    refCinemaVal: "Sci-Fi, Cyberpunk, Neo-noir",
    refBooks: "Books",
    refBooksVal: "Sci-Fi, Info Design",
    refGames: "Games",
    refGamesVal: "Puzzle, Indies, Art Direction",
    projectsLabel: "Projects",
    projectsTitle: "Real projects already published",
    projectsIntro:
      "Repositories that already show hands-on work with data, interface, automation and technical experimentation.",
    project1Status: "Published",
    project1Title: "Agricultural Database",
    project1Text:
      "Agricultural data structuring system with optimized tables, analysis queries, and crop visualization.",
    project2Status: "Published",
    project2Title: "Visual Interface for CLI",
    project2Text:
      "Custom terminal with QML GUI, Nordic theme, and visual process management on Linux.",
    project3Status: "Published",
    project3Title: "Neural Network Faux",
    project3Text:
      "Neural network built from scratch in Python — architecture, training, and learning visualization.",
    projectLink: "View on GitHub",
    visualLabel: "Visual",
    visualTitle: "Graphic work and creative presence",
    visualIntro:
      "My Behance profile concentrates the visual side of my portfolio. Here I leave direct access and space to highlight specific pieces in the next update.",
    visualProfileLabel: "Featured work",
    visualProfileTitle: "Vem Ser Tahto Works",
    visualProfileText:
      "Visual pieces created during my graphic design experience — branding, social media and internal materials. All gathered on Behance.",
    visualProfile2Title: "Visual Project",
    visualProfile2Text:
      "Visual identity, typography, and personal graphic compositions portfolio published on Behance.",
    visualPrimaryCta: "Open project on Behance",
    visualSecondaryCta: "View all repositories",
    visualNextLabel: "Behance",
    visualNextTitle: "Add more pieces with context",
    visualNextText:
      "I can keep bringing more Behance work here, one card at a time, with a short description, piece type and direct link.",
    educationLabel: "Education",
    educationTitle: "Data Science in progress",
    educationText: "Anhanguera · ongoing degree focused on analytics, data and technology.",
    certLabel: "Certification",
    roadmapLabel: "Next steps",
    roadmapTitle: "Planned upgrades for the published version",
    roadmap1Title: "Real projects",
    roadmap1Text: "Add Python scripts, automations, dashboards and AI studies.",
    roadmap2Title: "Visual portfolio",
    roadmap2Text: "Include graphic design and social media work with images and context.",
    roadmap3Title: "Publishing",
    roadmap3Text: "Deploy on GitHub Pages, Vercel or Netlify with a custom domain.",
    contactLabel: "Contact",
    contactTitle: "Let's talk about work, projects or collaboration.",
    contactText:
      "If the role or project needs someone who can organize, execute and learn fast, this is the right starting point.",
    footerQuote: "\"A man's dreams never end.\"",
    footerText:
      "Pedro Vitor's personal portfolio. Last updated on May 31, 2026.",
    footerStatus: "Available for opportunities",
    floatingCta: "Talk to me"
  }
};

const langToggle = document.querySelector("#langToggle");
const langElements = document.querySelectorAll("[data-i18n]");
const metaDescription = document.querySelector('meta[name="description"]');
const ogTitle = document.querySelector('meta[property="og:title"]');
const ogDescription = document.querySelector('meta[property="og:description"]');
const ogLocale = document.querySelector('meta[property="og:locale"]');
const savedLanguage = localStorage.getItem("portfolio-language");
const browserLanguage = navigator.language.toLowerCase().startsWith("pt") ? "pt" : "en";

function setLanguage(language) {
  const selected = translations[language] ? language : "pt";

  langElements.forEach((element) => {
    const key = element.dataset.i18n;
    if (translations[selected][key]) {
      if (key === "heroName") {
        element.innerHTML = translations[selected][key].replace("\n", "<br>");
      } else {
        element.textContent = translations[selected][key];
      }
    }
  });

  document.documentElement.lang = selected === "pt" ? "pt-BR" : "en";
  document.title = translations[selected].pageTitle;
  metaDescription?.setAttribute("content", translations[selected].pageDescription);
  ogTitle?.setAttribute("content", translations[selected].pageTitle);
  ogDescription?.setAttribute("content", translations[selected].pageDescription);
  ogLocale?.setAttribute("content", selected === "pt" ? "pt_BR" : "en_US");
  langToggle?.setAttribute(
    "aria-label",
    selected === "pt" ? "Mudar idioma para inglês" : "Switch language to Portuguese"
  );

  // Troca o link do currículo conforme o idioma
  const cvLink = document.getElementById("cvDownload");
  if (cvLink) {
    cvLink.href = selected === "pt" ? "./curriculo-pt.pdf" : "./curriculo-en.pdf";
  }

  localStorage.setItem("portfolio-language", selected);
}

langToggle.addEventListener("click", () => {
  langToggle.classList.add("lang-spinning");
  
  // Troca o idioma no meio da animação (quando o texto encolheu e ficou invisível)
  setTimeout(() => {
    const current = localStorage.getItem("portfolio-language") || browserLanguage;
    setLanguage(current === "pt" ? "en" : "pt");
  }, 250);

  setTimeout(() => {
    langToggle.classList.remove("lang-spinning");
  }, 500);
});

setLanguage(savedLanguage || browserLanguage);

// --- Theme Toggle (Dark/Light) ---
const themeToggle = document.getElementById("themeToggle");
const themeIcon = document.getElementById("themeIcon");
const body = document.body;

function setTheme(dark) {
  if (dark) {
    body.classList.add("dark");
    document.documentElement.classList.add("dark");
    themeIcon.className = "ph ph-sun";
    document.querySelector('meta[name="theme-color"]')?.setAttribute("content", "#1a1a1c");
  } else {
    body.classList.remove("dark");
    document.documentElement.classList.remove("dark");
    themeIcon.className = "ph ph-moon";
    document.querySelector('meta[name="theme-color"]')?.setAttribute("content", "#faf9f7");
  }
  localStorage.setItem("portfolio-theme", dark ? "dark" : "light");
}

// Check saved preference or system preference
const savedTheme = localStorage.getItem("portfolio-theme");
const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;

if (savedTheme === "dark" || (!savedTheme && prefersDark)) {
  setTheme(true);
}

themeToggle?.addEventListener("click", (e) => {
  themeToggle.classList.add("theme-spinning");
  setTimeout(() => {
    themeToggle.classList.remove("theme-spinning");
  }, 500);

  const isDark = body.classList.contains("dark");
  const nextDark = !isDark;

  // Se o navegador não suportar View Transitions ou preferir redução de movimento
  if (!document.startViewTransition || window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
    setTheme(nextDark);
    return;
  }

  // Obter as coordenadas do clique (ou o centro do botão como fallback)
  const rect = themeToggle.getBoundingClientRect();
  const x = e.clientX ?? (rect.left + rect.width / 2);
  const y = e.clientY ?? (rect.top + rect.height / 2);

  // Calcular o raio para cobrir toda a tela
  const endRadius = Math.hypot(
    Math.max(x, window.innerWidth - x),
    Math.max(y, window.innerHeight - y)
  );

  const transition = document.startViewTransition(() => {
    setTheme(nextDark);
  });

  transition.ready.then(() => {
    const clipPath = [
      `circle(0px at ${x}px ${y}px)`,
      `circle(${endRadius}px at ${x}px ${y}px)`
    ];
    
    // Se for para o dark mode, a nova tela (escura) cresce por cima.
    // Se for para o light mode, a antiga tela (escura) encolhe para revelar a clara.
    document.documentElement.animate(
      {
        clipPath: nextDark ? clipPath : [...clipPath].reverse()
      },
      {
        duration: 500,
        easing: "cubic-bezier(0.16, 1, 0.3, 1)",
        pseudoElement: nextDark ? "::view-transition-new(root)" : "::view-transition-old(root)"
      }
    );
  });
});

// --- Mobile Menu Toggle ---
const menuToggle = document.getElementById("menuToggle");
const topnav = document.getElementById("topnav");

menuToggle?.addEventListener("click", () => {
  topnav.classList.toggle("open");
  const isOpen = topnav.classList.contains("open");
  menuToggle.querySelector("i").className = isOpen ? "ph ph-x" : "ph ph-list";
  menuToggle.setAttribute("aria-label", isOpen ? "Fechar menu" : "Abrir menu");
  
  // Evita layout espremido removendo .scrolled se o menu mobile for aberto
  const topbar = document.querySelector(".topbar");
  if (topbar) {
    if (isOpen) {
      topbar.classList.remove("scrolled");
    } else if (window.scrollY > 40) {
      topbar.classList.add("scrolled");
    }
  }
});

// Close menu when a nav link is clicked
topnav?.querySelectorAll("a").forEach(link => {
  link.addEventListener("click", () => {
    topnav.classList.remove("open");
    menuToggle.querySelector("i").className = "ph ph-list";
    
    // Readquire o estado compactado do topbar se estiver scrolled
    const topbar = document.querySelector(".topbar");
    if (topbar && window.scrollY > 40) {
      topbar.classList.add("scrolled");
    }
  });
});

// --- Scroll Animations (Intersection Observer) ---
document.addEventListener("DOMContentLoaded", () => {
  const observerOptions = {
    root: null,
    rootMargin: "0px",
    threshold: 0.05
  };

  const observer = new IntersectionObserver((entries, observer) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add("visible");
        observer.unobserve(entry.target); // Anima apenas uma vez
      }
    });
  }, observerOptions);

  const animatedSections = document.querySelectorAll(".section-animate");
  animatedSections.forEach(section => {
    observer.observe(section);
  });

  // --- Scroll Spy (Active Nav Link) ---
  const navLinks = document.querySelectorAll(".topnav a[href^='#']");
  const sections = document.querySelectorAll("section[id]");

  function updateActiveNav() {
    const scrollY = window.scrollY + 200;

    sections.forEach(section => {
      const top = section.offsetTop;
      const height = section.offsetHeight;
      const id = section.getAttribute("id");

      if (scrollY >= top && scrollY < top + height) {
        navLinks.forEach(link => {
          link.classList.remove("active");
          if (link.getAttribute("href") === `#${id}`) {
            link.classList.add("active");
          }
        });
      }
    });
  }

  window.addEventListener("scroll", updateActiveNav, { passive: true });

  // --- Back to Top Button ---
  const backToTop = document.getElementById("backToTop");
  if (backToTop) {
    window.addEventListener("scroll", () => {
      if (window.scrollY > 600) {
        backToTop.classList.add("visible");
      } else {
        backToTop.classList.remove("visible");
      }
    }, { passive: true });

    backToTop.addEventListener("click", () => {
      window.scrollTo({ top: 0, behavior: "smooth" });
    });
  }

  // --- Sticky Topbar Scrolled Class ---
  const topbar = document.querySelector(".topbar");
  const topnavMenu = document.getElementById("topnav");
  if (topbar && topnavMenu) {
    window.addEventListener("scroll", () => {
      if (window.scrollY > 40 && !topnavMenu.classList.contains("open")) {
        topbar.classList.add("scrolled");
      } else {
        topbar.classList.remove("scrolled");
      }
    }, { passive: true });
  }

  // --- CV Download Button Micro-interaction ---
  const cvLink = document.getElementById("cvDownload");
  if (cvLink) {
    cvLink.addEventListener("click", () => {
      const icon = cvLink.querySelector("i");
      const span = cvLink.querySelector("span");
      const currentLang = localStorage.getItem("portfolio-language") || "pt";
      
      if (icon && span) {
        const originalIconClass = icon.className;
        const originalText = span.textContent;
        
        icon.className = "ph ph-check";
        span.textContent = currentLang === "pt" ? "Baixado!" : "Downloaded!";
        cvLink.classList.add("downloaded-success");
        
        setTimeout(() => {
          icon.className = originalIconClass;
          span.textContent = originalText;
          cvLink.classList.remove("downloaded-success");
        }, 2000);
      }
    });
  }

  // --- Typewriter Effect ---
  const heroTitle = document.querySelector(".hero h1.typewriter");
  if (heroTitle) {
    const fullHTML = heroTitle.innerHTML;
    heroTitle.innerHTML = '<span class="typewriter-cursor blinking"></span>';
    heroTitle.style.visibility = "visible";

    // Flatten text from HTML (preserving <br>)
    const parts = fullHTML.split("<br>");
    let charIndex = 0;
    let partIndex = 0;
    let currentText = "";

    function typeChar() {
      if (partIndex < parts.length) {
        const part = parts[partIndex];
        if (charIndex < part.length) {
          const char = part[charIndex];
          currentText += char;
          charIndex++;
          
          // Ritmo de digitação realista de computador (60ms a 120ms)
          let delay = 60 + Math.random() * 60;
          
          // Pausa extra em espaços e pontuação para simular digitação humana
          if (char === " ") {
            delay = 140 + Math.random() * 40;
          } else if (char === "," || char === "." || char === "-") {
            delay = 250 + Math.random() * 50;
          }

          // Cursor fica sólido (sem piscar) enquanto está ativamente digitando
          heroTitle.innerHTML = currentText + '<span class="typewriter-cursor"></span>';
          setTimeout(typeChar, delay);
        } else {
          if (partIndex < parts.length - 1) {
            currentText += "<br>";
            // Pisca o cursor durante a pausa da quebra de linha
            heroTitle.innerHTML = currentText + '<span class="typewriter-cursor blinking"></span>';
          }
          partIndex++;
          charIndex = 0;
          setTimeout(typeChar, 400); // Pausa de 400ms ao final de cada linha (computador)
        }
      } else {
        // Digitação finalizada -> pisca e depois faz o fade-out
        heroTitle.innerHTML = currentText + '<span class="typewriter-cursor blinking finished"></span>';
        heroTitle.classList.add("done");
        
        // Inicia a digitação das atribuições (kicker) após 1 segundo
        setTimeout(startKickerTypewriter, 1000);
      }
    }

    function startKickerTypewriter() {
      const kicker = document.querySelector(".hero-kicker");
      if (!kicker) return;
      
      const currentLang = localStorage.getItem("portfolio-language") || "pt";
      
      // Lista de atribuições dinâmicas
      const words = currentLang === "pt" ? [
        "Cientista de Dados",
        "Desenvolvedor Python",
        "Designer Gráfico",
        translations[currentLang].heroKicker
      ] : [
        "Data Scientist",
        "Python Developer",
        "Graphic Designer",
        translations[currentLang].heroKicker
      ];
      
      kicker.innerHTML = '<span class="kicker-text"></span><span class="typewriter-cursor blinking"></span>';
      kicker.style.visibility = "visible";
      
      const textEl = kicker.querySelector(".kicker-text");
      const cursorEl = kicker.querySelector(".typewriter-cursor");
      
      let wordIndex = 0;
      let charIndex = 0;
      let isDeleting = false;
      
      function typeKicker() {
        const currentWord = words[wordIndex];
        
        if (isDeleting) {
          charIndex--;
          textEl.textContent = currentWord.substring(0, charIndex);
          cursorEl.classList.remove("blinking");
        } else {
          charIndex++;
          textEl.textContent = currentWord.substring(0, charIndex);
          cursorEl.classList.remove("blinking");
        }
        
        let delay = isDeleting ? 30 : 60 + Math.random() * 40;
        
        if (!isDeleting && charIndex === currentWord.length) {
          cursorEl.classList.add("blinking");
          if (wordIndex === words.length - 1) {
            // Se for a última frase, finaliza piscando e depois esmaece
            setTimeout(() => {
              cursorEl.classList.add("finished");
            }, 1500);
            return;
          }
          isDeleting = true;
          delay = 1500; // Tempo de exibição da palavra antes de apagar
        } else if (isDeleting && charIndex === 0) {
          isDeleting = false;
          wordIndex++;
          delay = 400; // Tempo de espera com o campo limpo
        }
        
        setTimeout(typeKicker, delay);
      }
      
      typeKicker();
    }

    // Inicia a digitação após um pequeno delay inicial (cursor fica piscando no início)
    setTimeout(typeChar, 800);
  }

  // --- Particle Canvas ---
  const canvas = document.getElementById("particleCanvas");
  if (canvas) {
    const ctx = canvas.getContext("2d");
    let particles = [];
    const PARTICLE_COUNT = 50;
    const MAX_DIST = 120;

    function resizeCanvas() {
      const hero = canvas.parentElement;
      canvas.width = hero.offsetWidth + 40;
      canvas.height = hero.offsetHeight;
    }

    function createParticles() {
      particles = [];
      for (let i = 0; i < PARTICLE_COUNT; i++) {
        particles.push({
          x: Math.random() * canvas.width,
          y: Math.random() * canvas.height,
          vx: (Math.random() - 0.5) * 0.5,
          vy: (Math.random() - 0.5) * 0.5,
          r: Math.random() * 2 + 1
        });
      }
    }

    function drawParticles() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      const isDark = document.body.classList.contains("dark");
      const color = isDark ? "rgba(212, 129, 94," : "rgba(201, 111, 74,";

      for (let i = 0; i < particles.length; i++) {
        const p = particles[i];
        p.x += p.vx;
        p.y += p.vy;

        if (p.x < 0 || p.x > canvas.width) p.vx *= -1;
        if (p.y < 0 || p.y > canvas.height) p.vy *= -1;

        ctx.beginPath();
        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
        ctx.fillStyle = color + "0.4)";
        ctx.fill();

        for (let j = i + 1; j < particles.length; j++) {
          const p2 = particles[j];
          const dx = p.x - p2.x;
          const dy = p.y - p2.y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < MAX_DIST) {
            ctx.beginPath();
            ctx.moveTo(p.x, p.y);
            ctx.lineTo(p2.x, p2.y);
            ctx.strokeStyle = color + (0.15 * (1 - dist / MAX_DIST)) + ")";
            ctx.lineWidth = 0.5;
            ctx.stroke();
          }
        }
      }
      requestAnimationFrame(drawParticles);
    }

    resizeCanvas();
    createParticles();
    drawParticles();
    window.addEventListener("resize", () => {
      resizeCanvas();
      createParticles();
    });
  }

  // --- Floating CTA ---
  const floatingCta = document.getElementById("floatingCta");
  if (floatingCta) {
    window.addEventListener("scroll", () => {
      if (window.scrollY > 400) {
        floatingCta.classList.add("visible");
      } else {
        floatingCta.classList.remove("visible");
      }
    }, { passive: true });
  }

  // --- Scroll Indicator Fade-out ---
  const scrollIndicator = document.querySelector(".scroll-indicator");
  if (scrollIndicator) {
    window.addEventListener("scroll", () => {
      const scrollY = window.scrollY;
      const opacity = Math.max(0, 1 - scrollY / 150);
      scrollIndicator.style.opacity = opacity;
      if (opacity <= 0) {
        scrollIndicator.style.pointerEvents = "none";
      } else {
        scrollIndicator.style.pointerEvents = "auto";
      }
    }, { passive: true });
  }

  // --- Spotlight Hover Glow Effect ---
  const cards = document.querySelectorAll('.project-card, .visual-card, .contact-box, .bento-card');
  cards.forEach(card => {
    card.addEventListener('mousemove', e => {
      const rect = card.getBoundingClientRect();
      const x = e.clientX - rect.left;
      const y = e.clientY - rect.top;
      card.style.setProperty('--mouse-x', `${x}px`);
      card.style.setProperty('--mouse-y', `${y}px`);
    });
  });

  // --- Magnetic Buttons Effect ---
  if (window.matchMedia("(hover: hover)").matches) {
    const magneticBtns = document.querySelectorAll(".magnetic-btn");
    magneticBtns.forEach(btn => {
      btn.addEventListener("mousemove", (e) => {
        const rect = btn.getBoundingClientRect();
        const centerX = rect.left + rect.width / 2;
        const centerY = rect.top + rect.height / 2;
        
        const deltaX = e.clientX - centerX;
        const deltaY = e.clientY - centerY;
        
        // Força da atração (coeficiente de deslocamento)
        const strength = 0.35;
        const moveX = deltaX * strength;
        const moveY = deltaY * strength;
        
        btn.style.setProperty("--magnetic-x", `${moveX}px`);
        btn.style.setProperty("--magnetic-y", `${moveY}px`);
        
        // Efeito Parallax interno no ícone do botão
        const icon = btn.querySelector("i");
        if (icon) {
          icon.style.transform = `translate(${moveX * 0.15}px, ${moveY * 0.15}px)`;
          icon.style.transition = "transform 0.1s ease";
        }
      });
      
      btn.addEventListener("mouseleave", () => {
        btn.style.setProperty("--magnetic-x", "0px");
        btn.style.setProperty("--magnetic-y", "0px");
        
        const icon = btn.querySelector("i");
        if (icon) {
          icon.style.transform = "";
          icon.style.transition = "transform 0.3s ease";
        }
      });
    });
  }
});
